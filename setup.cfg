[flake8]
max-line-length = 120
# E203: whitespace before ':' (conflicts with slicing style), W503: line break before binary operator.
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    build,
    dist,
    .eggs,
    *.egg-info
